import { Box, Button } from "@chakra-ui/react";

export default function Index() {
  return (
    <div>
      <Box mt={20} mx="auto">
        <Button w={["100%", "initial"]} variant={"solid"}>
          Clicar
        </Button>
      </Box>
    </div>
  );
}
