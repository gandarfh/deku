import { ButtonProps, ComponentSingleStyleConfig } from "@chakra-ui/react";

export const Button: ComponentSingleStyleConfig = {
  baseStyle: {
    fontWeight: "bold",
    borderRadius: "md",
  },
};
